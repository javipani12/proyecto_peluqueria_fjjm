import 'package:flutter/material.dart';

class EnvioScreen extends StatelessWidget {
  const EnvioScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('EnvioScreen'),
      ),
    );
  }
}
