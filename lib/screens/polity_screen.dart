
import 'package:flutter/material.dart';

class PolityScreen extends StatelessWidget {
  const PolityScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('PolityScreen'),
      ),
    );
  }
}
